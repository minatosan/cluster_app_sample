require 'rails_helper'

RSpec.describe "Groups", type: :request do
  
  describe "GET /index" do
    #user = FactoryBot.create(:user)
    #user = FactoryBot.create(:user)
    #login_user user
    #get :index
    #get groups_path
    #expect(response.status).to eq(200)
  end

  describe "create" do
    it "グループを作成すると、admin_user_idに追加されるのか？" do

    end    
  end
end
